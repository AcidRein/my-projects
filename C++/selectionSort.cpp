#include <bits/stdc++.h>
using namespace std;

vector<int> selectionSort(vector<int> arrayParam) {
    leftPointer *p 

}

int main(void) {
    vector<int> sortMe = {5, 4, 1, 2, 3, 0, 9, 13, 145, 32, 592};

    selectionSort(sortMe);

    // Print
    cout << "[";
    for (auto i : sortMe) {
        cout << i << " ";
    }

    cout << "]" << endl;
}